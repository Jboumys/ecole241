<table class="table text-center">
               <tr>
                   <th>Matricule</th>
                   <th>date création</th>
                   <th>Mise à jour le</th>
                   <th>Propriétaire</th>
                   <th colspan="3">Action</th>
               </tr>
               {% for dossier in dossiers %}
                   <tr>
                       <td>{{ dossier.matricule }}</td>
                       <td>{{ dossier.dateCreate|date('d-m-Y') }}</td>
                       <td>{{ dossier.dateMAJ|date('d-m-Y') }}</td>
                       <td>{{ dossier.requerant.identite}}</td>
                       <td><a class="btn btn-primary btn-sm" href="{{ path('dossier_show',{id: dossier.id}) }}">Voir</a></td>
                       <td><a class="btn btn-success btn-sm" href="">Editer</a></td>
                       <td><a class="btn btn-danger btn-sm" href="">Supprimer</a></td>
                   </tr>
               {% endfor %}
           </table>