 <div>
                            <table class="table">
                                <tr>
                                    <th>Nom</th>
                                    <th>Prénom</th>
                                    <th>Genre</th>
                                    <th>Adresse</th>
                                    <th class="text-center" colspan="3">Action</th>
                                </tr>
                                {% for requerant in requerants %}
                                <tr>
                                    <td>{{ requerant.nom }}</td>
                                    <td>{{ requerant.prenom }}</td>
                                    <td>{{ requerant.genre }}</td>
                                    <td>{{ requerant.adresse }}</td>
                                    <td><a href="#" class="btn btn-primary btn-sm">Voir</a></td>
                                    <td><a href="#" class="btn btn-success btn-sm">Editer</a></td>
                                    <td><a href="#" class="btn btn-danger btn-sm">Supp</a></td>
                                </tr>
                                {% endfor %}
                            </table>
                            <br/>
                        </div>






                        {% extends 'base.html.twig' %}

                        {% block body %}

                            <div class="card text-center">
                                <div class="card-body">
                                    <br/>
                                    {# read and display just one flash message type #}
                                    <div id="message">
                                        {% for message in app.flashes('success') %}
                                            <div class="alert alert-success text-center">
                                                {{ message }}✅
                                            </div>
                                        {% endfor %}
                                        {% for message in app.flashes('warming') %}
                                            <div class="alert alert-warning text-center">
                                                {{ message }}
                                            </div>
                                        {% endfor %}
                                    </div>


                                    <h1 class="text-center">Nouveau dossier</h1><br/>

                                    <div class="row">
                                        <div class="col-md-12">
                                            {{ form_start(form) }}
                                            <div class="col-md-6">
                                                <div class="form-group">
                                                    {{ form_row(form.matricule, {'attr': {'class': 'form-control'}}) }}
                                                </div>
                                                <div class="form-group">
                                                    {{ form_row(form.requerant, {'attr': {'class': 'form-control'}}) }}
                                                </div>
                                            </div>
                                            <div class="col-md-6">
                                                <div class="form-group">
                                                    {{ form_row(form.parcelle, {'attr': {'class': 'form-control'}}) }}
                                                </div>
                                            </div>
                                            <div class="form-group">
                                                <button type="submit" class="btn btn-primary">Créer</button>
                                            </div>
                                            {{ form_end(form) }}
                                        </div>
                                    </div>

                                </div>
                            </div>

                        {% endblock %}